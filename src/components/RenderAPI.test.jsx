import {render, screen} from '@testing-library/react'
import RenderAPI from './RenderAPI'
import {test, expect, describe} from 'vitest'


describe('Render API', () => {
  test('initial render and the api returns a valid response', () => {
    
  })

  test('displays error message if does not render', () => {

  })

  test('verifies the getName function', () => {

  })

  test('renders the image', () => {

  })
})
